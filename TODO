Import API's Library
Detect if there's internet connection for importing latest CDN's
Have default settings
Allow users to input string of libraries to install and find them and publish them if found. 